# Memory — 2026-05-10

## Harris Bros — Offboarded

- Rowdy couldn't continue financially. Deal was 15% of revenue Zak brought in (Rowdy wanted 10%, Zak said no).
- Tenant `harris_bros` (slug: `harrisbrosads`) DISABLED in Swell DB.
- Password changed to `nopressurelaunchstop`.
- Lead sync will never pull for them again.
- 24 historical leads remain in DB, frozen.

## Swell — MackWash Discord Fix

- Lead #50 (Faisal Mohammad, May 10) came in but had no Discord thread due to expired bot token.
- Root cause: Hayden bot token expired/revoked. New token provided by Zak and updated in `.env`.
- Backfilled thread for Lead #50 (thread ID: 1503197461764767784) — lead card + 9-message conversation history posted.
- Lead #50 was legitimately out of service area (Acworth GA). Hayden handled correctly.

## Swell — Discord Gateway Hardening

- Exponential backoff added to `swell/server/services/discord-gateway.ts` (5s→10s→20s→40s→60s, max 5 retries).
- Hard-stop on "disallowed intents" (code 4014) — prevents reconnection storm.
- Self-healing `run.sh` wrapper added to Swell: if node crashes, bash restarts it with backoff.

## HH Backend — Discord Routing Overhaul (May 10)

### New WPW Discord Channels (added by Zak tonight)
- `#reviews` channel ID: `1503198049399341267` — review request sent notifications
- `#how-did-we-do` channel ID: `1503198360071569538` — per-customer review reply threads

### Reactivation Reply Routing (CHANGED)
- Old: only `human_handoff` → webhook → leads channel
- New: ALL replies → thread in **#reactivation** (1502357456452587612)
- New: `human_handoff` → also alerts **#updates** (1502363455779504198) with @Matthew tag + thread link
- `#convos` threads channel replaced by #reactivation threads — Zak can delete `#convos`
- Backfill ran: all 8 existing replies got threads in #reactivation, 5 human_handoffs alerted to #updates

### Review Reply Routing
- Review replies → thread in #how-did-we-do
- Review sent notifications → #reviews

### discord-handoff.ts rewritten
- Now uses bot API directly (not webhook) for all Discord posts
- Both `postReactivationHandoff` and `postReviewReplyToThread` support `existingThreadId` to append vs create

## HH Backend — Error Handling Hardened

- `uncaughtException` + `unhandledRejection` handlers added to both Swell and HH backend
- Graceful SIGTERM/SIGINT shutdown added
- Self-healing `run.sh` wrappers added to both servers
- Logs crash reason before restart

## HH Backend — Discord Gateway (Scout)

- Gateway still failing: `MessageContent intent not enabled in Discord Developer Portal`
- Zak said he saved permissions but the gateway is still returning code 4004 / disallowed intents
- Scout bot user ID: `1489673606966280443`
- Fix: discord.com/developers/applications → Scout app → Bot tab → Privileged Gateway Intents → Message Content Intent → ON
- REST API (notifications, thread posts) works fine without it — only two-way SMS bridge needs it

## WPW Review Campaign — Pre-Launch State

- Campaign #3 "WPW Review Request — Variant A (Personal)": 42 sends queued
- Campaign #4 "WPW Review Request — Variant B (Social Proof)": 41 sends queued
- Total: 83 sends ready
- `to_name` issue found: all had "there" instead of customer names — to be fixed before launch
- Launch scheduled: **9:00 AM EDT May 11** (drip 5/hour throughout day)
- End-of-day A/B test report scheduled for delivery at 8pm EDT
- Review link: `https://g.page/r/CVeCBRNnDF0_EBM/review` ✅ confirmed working

## WPW Reactivation Campaign — Current Results (as of May 10 EOD)

- 16 sent, 4 replied (25% reply rate — strong)
- 2 complaints: "I was overcharged and nobody responded" + "Actually you were a no show"
- 4 hot leads: interested in scheduling
- 2 polite declines
- 5 sends failed (bad data in source records)
- 10 sends still queued (next batch)
- Malcolm Al-Amin DNCed after requesting info deletion
- All replies backfilled to #reactivation threads + #updates alerts fired

## AI Reply Analysis — NOT YET BUILT (Phase 2)

- Current classifier is rule-based keyword matching only
- Lead status NOT auto-updated on reply
- Matthew gets same alert regardless of reply sentiment
- Zak was asked if he wants this built (Haiku call → intent label → lead status update → richer Discord alert)
- Decision pending

## Swell — Lead Sync Behavior

- Sync runs every 1 HOUR (not every 30 min — the code comment says "hourly")
- Health check cron fires every 30min to check the server is up
- Harris Bros excluded from sync (disabled)
- MackWash: fully configured, hourly sync active

## Key Env Vars Added/Changed Tonight

### Swell `.env`
- `DISCORD_BOT_TOKEN` — updated to new Hayden token (Zak regenerated 5/10 evening)

### HH Backend `.env` (to add/confirm)
- `DISCORD_REACTIVATION_CHANNEL_ID=1502357456452587612`
- `DISCORD_UPDATES_CHANNEL_ID=1502363455779504198`
- `DISCORD_REVIEWS_CHANNEL_ID=1503198049399341267`
- `DISCORD_REVIEWS_CONVOS_CHANNEL_ID=1503198360071569538`
- `DISCORD_MATTHEW_USER_ID` — Zak needs to provide Matthew's numeric Discord user ID

## Tomorrow's Agenda (May 11, 2026)

1. **Content** — get content squared away across brands (specifics TBD)
2. **Mop Mafia FB ads** — get ads running and integrated with Swell/Gia pipeline
3. **SAS system revamp** — full analysis of the current Sales Automation System, safe revamp without breaking existing flows
4. **Content scheduling** — WPW and Healthy Home content calendars built and queued
5. **Serviced jobs audit** — pull from HH backend what jobs got serviced, which customers, revenue
6. **Business opportunity analysis** — analyze all data across WPW, HH, MackWash for highest-leverage next moves

Priority order (suggested): Start with serviced jobs audit + opportunity analysis first (data-driven, sets context for everything else) → then Mop Mafia ads → then content scheduling → SAS revamp last (most careful, needs dedicated focus)

7. **Blue Ocean client acquisition brainstorm** — find highest-leverage ways to land more clients for Blue Ocean ($1,200/mo retainer model, home services niche)
