# 2026-04-21

- Continued Healthy Home property enrichment work for Wolf Pack Wash -> HH CRM flow.
- Added schema scaffolding in `hh-backend-fresh/lib/db/src/schema/canvassing.ts` for:
  - customer-submitted/canonical property fields: `requestedTimeline`, `exactServiceRequested`, `propertyStories`, `gutterLinearFeet`
  - property lookup state: status, source, confidence, failure reason, manual review flag, escalation timestamps/reason
  - system-estimated fields: estimated house sqft, cement sqft, stories, gutter LF, source/confidence
  - imagery-estimation groundwork: status, notes, asset list
- Updated web form + Facebook lead intake so new leads can persist timeline/exact service/house sqft and mark lookup as `pending` when address exists, or `failed` + manual review when address is missing.
- Updated canvassing lead API mapping so lead detail responses include the new enrichment fields and lead updates can persist manual fallback, escalation, and imagery-estimation state.
- Updated LeadDrawer UI so lead detail now surfaces requested service/timeframe, separates manual property data from system enrichment, and provides edit controls for:
  - manual fallback entry
  - failed-lookup/manual-review state
  - escalation to team when lookup fails
  - future imagery/cement estimation notes and status
- Validation: dashboard typecheck still fails due to pre-existing unrelated TS issues and missing built `lib/api-client-react` artifacts; no new targeted issue isolated from this slice.
- Product direction confirmed by Zak: when property lookup fails, support manual entry fallback plus escalation to the team, and continue building toward a future cement / imagery estimation feature.
# 2026-04-21

- Continued Healthy Home property enrichment work for Wolf Pack Wash -> HH CRM flow.
- Added schema scaffolding in `hh-backend-fresh/lib/db/src/schema/canvassing.ts` for:
  - customer-submitted/canonical property fields: `requestedTimeline`, `exactServiceRequested`, `propertyStories`, `gutterLinearFeet`
  - property lookup state: status, source, confidence, failure reason, manual review flag, escalation timestamps/reason
  - system-estimated fields: estimated house sqft, cement sqft, stories, gutter LF, source/confidence
  - imagery-estimation groundwork: status, notes, asset list
- Updated web form + Facebook lead intake so new leads can persist timeline/exact service/house sqft and mark lookup as `pending` when address exists, or `failed` + manual review when address is missing.
- Updated canvassing lead API mapping so lead detail responses include the new enrichment fields and lead updates can persist manual fallback, escalation, and imagery-estimation state.
- Updated LeadDrawer UI so lead detail now surfaces requested service/timeframe, separates manual property data from system enrichment, and provides edit controls for:
  - manual fallback entry
  - failed-lookup/manual-review state
  - escalation to team when lookup fails
  - future imagery/cement estimation notes and status
- Validation: dashboard typecheck still fails due to pre-existing unrelated TS issues and missing built `lib/api-client-react` artifacts; no new targeted issue isolated from this slice.
- Product direction confirmed by Zak: when property lookup fails, support manual entry fallback plus escalation to the team, and continue building toward a future cement / imagery estimation feature.
- Ad ops guidance for Showroom Google Ads: default recommendation is to keep AI Max off while the campaign is being dialed in; if left on, at least disable Final URL expansion, since tighter control is better for local-service lead quality early on.
