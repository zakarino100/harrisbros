# Memory — 2026-04-09

## Mop Mafia LP (book.mop-mafia.com)

- Rebuilt quote funnel flow: collect ALL info first (steps 1–5: frequency → type → size → addons → contact), THEN submit → calculating interstitial → price reveal screen
- Price reveal screen has two tracked CTAs: "Book My Clean Now" (logs `cta_click_book_now`) and "Call to Book: (984) 464-6019" (logs `cta_click_call_to_book` async, opens tel: link)
- `price_revealed` activity event auto-logs when price screen mounts
- UTM params captured on page load, stored on lead + included in all activity log metadata
- LP server has `/api/activity` endpoint that proxies to CRM activity webhook
- LP schema updated: added utm_source/medium/campaign/content/term to quoteFormSchema
- LP routes.ts: `sendActivityToCRM` sends to `https://mop-mafia-crm--zakarino100.replit.app/webhooks/landing-page/activity`
- LP routes.ts: CRM now returns `lead_id` in quote submission response
- Book Now button currently points to `https://book.mop-mafia.com/schedule` — placeholder, needs real booking URL

## Mop Mafia CRM (mop-mafia-crm--zakarino100.replit.app)

- Major build pushed 2026-04-09. GitHub: zakarino100/Mop-Mafia-CRM
- Schema additions (after `npm run db:push`):
  - `leads` table: added notes, frequency, homeSize, calculatedPrice, addons, utmSource/Medium/Campaign/Content/Term, actionTaken, actionTakenAt, lastSmsAlertAt
  - New `lead_activities` table (id, leadId FK cascade, actionType, metadata JSON, createdAt)
  - New `settings` table (key PK, value, updatedAt)
- Storage: createLeadActivity, getLeadActivities, getSetting, setSetting, getAllSettings
- New routes:
  - `GET /api/leads/:id/activities` — activity timeline per lead
  - `POST /api/calls/initiate` — Twilio click-to-call (calls owner first, bridges to lead)
  - `POST /webhooks/twilio/click-to-call` — TwiML for bridging
  - `POST /webhooks/landing-page/activity` — receives CTA events from LP
  - `GET/PUT /api/settings` — settings CRUD
- Landing page webhook updated: now stores frequency, homeSize, calculatedPrice, addons, UTMs
- Owner SMS alert: fires on new lead, 5-min debounce (uses lastSmsAlertAt), checks `owner_sms_enabled` setting
- Owner number: +13159357169, Mop Mafia Twilio number: +19844646019
- Frontend:
  - Leads page: clickable rows → Sheet slide-out with Overview/Activity/Notes tabs
  - Overview shows quote data, price in green, action taken badge, UTM source
  - Activity timeline: vertical list of all events with icons + time-ago
  - Call Now button per lead (click-to-call), Messages button
  - Mobile: card list replaces table on small screens
  - Settings page: Owner SMS toggle (live/functional), Lead Nurturing + Form SMS (Coming Soon toggles)
  - Settings added to sidebar nav
- `db:push` was run successfully on 2026-04-09 — all new tables/columns are live

## TradeWell (zakarino100/TradeWell)

- Trading education landing page + admin CRM + backend
- Stack: React/Vite (site), React/Vite (admin), Express/Node (backend), Postgres
- Colors: black (#0a0a0a) + gold (#C9A84C) — navy (#0B1C3D) was fully replaced
- Build output: `site/npm run build` → outputs to `backend/public/site/`
- Backend serves site at `/*` and admin at `/admin/*`

### Changes made 2026-04-09:

**Pricing hidden** — Pricing.jsx replaced with "Schedule Your Call" CTA section. No prices shown on site.

**Discovery call booking system:**
- Multi-step BookingModal: date (7-day week view) → time slots → name/phone/email → confirmation
- Rules: 4–8 PM ET only (30-min slots), Saturday always blocked, 48h minimum advance, ~3 slots/day appear taken (deterministic by DOW), 30-min conflict buffer enforced server-side
- Backend: `backend/routes/discovery.js` — public `/api/discovery/book` + `/api/discovery/available` + admin `/api/discovery`
- Schema: `tw_discovery_calls` table (needs manual SQL run — NOT auto-migrated)
- Backend registered: `app.use('/api/discovery', require('./routes/discovery'))`
- Booking error ("Unexpected token '<'") = backend needs restart + SQL table needs to be created manually

**SQL to run manually:**
```sql
CREATE TABLE IF NOT EXISTS tw_discovery_calls (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  lead_id UUID REFERENCES tw_leads(id),
  name TEXT NOT NULL, email TEXT NOT NULL, phone TEXT NOT NULL,
  scheduled_at TIMESTAMPTZ NOT NULL,
  status TEXT NOT NULL DEFAULT 'scheduled'
    CHECK (status IN ('scheduled', 'completed', 'cancelled', 'no_show')),
  notes TEXT,
  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT now()
);
```

**Solution.jsx tiles:**
- 4 tiles with unique SVG illustrations (visible gold decorations):
  - 01: scattered dots + horizontal slash line
  - 02: ascending bar chart + upward trend line with arrow
  - 03: concentric target rings + crosshairs + checkmark
  - 04: trading terminal grid + upward chart line with data dots
- Previous versions had invisible CSS gradients (black on black) — fixed with inline SVG

**Nav.jsx:**
- SVG logo with upward chart-arrow polyline + arrowhead
- "TRADE" text color shifts to gold on scroll (>60px), nav gets blur/shadow
- "Book a Call" button wires to `onBookingOpen`

**Hero.jsx:**
- Animated SVG line chart draws itself over 2s with pulsing live dot
- Stats: 50+ traders funded, 6mo guarantee, $0 hype
- CTA wired to booking modal

**Stats.jsx (new section):**
- Canvas confetti on first visit (localStorage key `tw_confetti_shown`)
- 3 animated SVG donut charts (84% pass rate, 91% reach payout, 78% return)
- Animate on scroll via IntersectionObserver
- Positioned between Problem and Solution sections

**App.jsx:**
- `onBookingOpen` prop threaded through Nav, Hero, Pricing, FinalCTA
- Stats section added to page order

**Favicon:** Chart-arrow monogram SVG (gold on black)

**OG meta tags:** Updated title, description, og:image, theme-color in index.html

**Replit git workflow notes:**
- TradeWell site Replit has divergent branches — always use `git stash && git pull origin main --rebase && git stash drop && npm run build`
- Build command is `npm run build` from `~/workspace/site/`
- Backend restart needed after pulling new routes

## Git/workflow patterns established

- When Replit has local changes diverging from GitHub: `git stash → git pull --rebase → git stash drop`
- When GitHub has secrets in commit history: `git rm -r --cached <folder> → git commit --amend → git push`
- When GitHub push protection blocks secrets: remove files from index, amend commit
- TradeWell, Mop-Mafia-CRM, Mop-Mafia-LP all on zakarino100 GitHub org

## Harris Bros Window Cleaning / Rowdy (new marketing client)

- New 30-day free-trial Meta ads client: Harris Bros Window Cleaning (also operates as Harris Brothers Pressure Wash & Gutter Clean, LLC)
- Initial market for ads: radius around Joplin, MO, with room to adjust based on performance
- GBP provided by Zak; public footprint shows services including window cleaning, gutter cleaning, pressure washing, and Christmas lights
- Answer doc reviewed: `~/Downloads/Rowdy ANSWERS.pdf`
- Pricing from Rowdy doc:
  - Exterior windows: 25 panes $295, 40 $395, 60 $495, 80 $645
  - Interior windows: 25 panes $145, 40 $195, 60 $245, 80 $345
  - Screens: 25 panes $50, 40 $100, 60 $150, 80 $200
- Recurring offers / differentiation:
  - Quarterly: $100 off + touchups + Rain Guard + 14-day rain guarantee
  - Bi-Annual: $50-$100 off depending on size
- Joplin service area from doc: Jasper, Newton, McDonald counties (MO), Ottawa County (OK), Cherokee County (KS)
- No website currently; lead handling expected to go direct to phone / lead form
- Budget/capacity decision from Zak: start at roughly $40-$50/day
- Leads should route to Rowdy's phone number
- The two before/after photos Zak sent are Rowdy's own before/after assets and should be treated as the base creative references
- Correct Harris Brothers logo direction: white serif "HB" monogram with "Harris Brothers" underneath on black background

## Harris Bros ad creative work

- Local folder created: `/Users/zak/.openclaw/workspace/CONTENT/Harris Bros/Ad Concepts`
- Desktop copy created: `~/Desktop/Harris Bros/Ad Concepts`
- Initial locally composed SVG concepts were not acceptable and should not be treated as final direction
- Gemini image generation became available again after credits were added
- Generated and saved fresh Harris Bros concept image:
  - Workspace: `/Users/zak/.openclaw/workspace/CONTENT/Harris Bros/Ad Concepts/harris-bros-gemini-concepts-01.jpg`
  - Desktop: `~/Desktop/Harris Bros/Ad Concepts/harris-bros-gemini-concepts-01.jpg`
- Generated and saved more aggressive / more Meta-like second batch using the corrected black-background HB/Harris Brothers logo direction:
  - Workspace: `/Users/zak/.openclaw/workspace/CONTENT/Harris Bros/Ad Concepts/harris-bros-gemini-concepts-02.jpg`
  - Desktop: `~/Desktop/Harris Bros/Ad Concepts/harris-bros-gemini-concepts-02.jpg`
- Creative strategy direction for Harris Bros:
  - lead with transformation, clarity, and local trust
  - do NOT lead with full pricing matrix in cold ads
  - strongest likely concepts: direct before/after, emotional clarity, Joplin local trust, spring reset / seasonal cleanup

## Mop Mafia current ad / call quality concern

- Zak reports roughly $100 ad spend with only 1 form submission so far — campaign quality needs tightening immediately
- Zak confirmed repeated spam / nuisance calls are coming through the Mop Mafia Twilio CRM system
- Zak confirmed those repeat spam calls are NOT currently visible as ad-attributed leads in CRM
- Important product gap identified: Mop Mafia CRM does not yet show which inbound calls came from ads, and Zak wants that attribution added
- Best course identified:
  1. build spam-call detection / scoring inside Mop Mafia CRM
  2. add workflow for mark-as-spam / bulk spam review / possible blocklist
  3. set up cleaner call attribution, ideally with a dedicated Meta tracking number forwarded into Twilio/CRM so ad-driven calls can be separated from organic/spam traffic
- Short-term priority: tighten Mop Mafia funnel, call attribution, and spam handling aggressively rather than continuing to spend blindly

## Updated practical to-do list

- TradeWell:
  - On Replit, restart backend and run manual SQL for `tw_discovery_calls`
  - Verify `/api/discovery/book` and `/api/discovery/available` return JSON instead of HTML
  - Confirm booking flow works end-to-end after rebuild/restart
- Harris Bros:
  - Review generated concept batch 02 and identify strongest directions
  - Generate additional fresh Meta-style creative batches once direction is chosen
  - Build final copy/headline/body/lead-form package for first campaign launch
- Mop Mafia:
  - Investigate actual Twilio call logs / repeated caller patterns
  - Add CRM spam detection and spam review workflow
  - Add ad-call attribution path, ideally via dedicated tracking number
  - Tighten Meta campaign due to weak early performance ($100 spend / 1 form lead)
