# Memory — 2026-05-03

## Swell SMS Workflow Testing Session

### What we did
- Tested the full Swell AI SMS workflow end-to-end for Harris Bros and MackWash
- Dashboard password: `nopressure` (shared by Zak)
- Test phone used: +13154470614 (Zak's number)
- Used `/api/test/real-loop` endpoint (requires auth cookie via `/login` + `x-tenant-slug` header)
- Tenant IDs: `harris_bros`, `mackwash`; login slug for harris bros is `harrisbrosads`, mackwash is `mackwash`
- Swell Replit SSH: `ssh -i ~/.ssh/replit -p 22 ef40b1f4-73d3-45c4-9fa5-3c4d156824d5@ef40b1f4-73d3-45c4-9fa5-3c4d156824d5-00-sosmtk9rwo1p.kirk.replit.dev`
- Build: `npm run build` then `pkill node && nohup node dist/server/index.js > /tmp/swell.log 2>&1 &`

---

## Swell fixes shipped today

1. **Handoff pauses AI** — conversation.status === 'handoff' blocks runAssistantTurn. Inbound still logged + mirrored to Discord.
2. **Resume AI toggle** — `PATCH /api/messages/:id/resume-ai`. Dashboard shows amber banner + "▶ Resume AI" button.
3. **Pricing backfill** — seed.ts now backfills blank AI configs (0 services) on boot.
4. **Discord double booking** — removed duplicate notifyBookingDiscord call. notifyHandoffDiscord is single source.
5. **Leads channel handoff ping** — @here alert posted to leads channel on every handoff. Non-booking: leads only. Booking: leads ping + bookings thread.
6. **Schedule → Pending Bookings queue** — strip at top of Schedule page shows all ready-to-book leads as cards. Click card → select it → click a calendar day → confirm modal with time/notes → creates appointment.
7. **Route Builder tab** — new "Route" tab in Schedule (next to Week/Month). Leaflet map, numbered pins, gray=unscheduled/gold=in-route/blue=existing, polyline, sidebar with job details + travel time estimates, "Book This Route" → creates all appointments, "Send Scheduling SMS" per job fires Twilio confirmation text to customer.

## Swell conversation improvements

### MackWash AI config (all updates pushed to DB + seed-ai.ts)
- Pricing: strictly $150/hr. Services: house wash by sqft tier (under 1500/1500-2500/2500-4000/4000+), driveway, walkway — DO NOT quote driveway proactively (must measure on-site)
- Checking route flow: after getting address + sqft → send "Got it — give me one second to check my route." then `<<HOLD>>` then full quote (quote sent 3 min later via setTimeout)
- Quote format: "If I can get you on the route I have out in [city] next week, I can do $[price] for the whole house wash, top to bottom — covers all the siding, trim, soffit and eaves, and the exterior windows and screens."
- After pitch: STOP, wait. Positive → "Does Monday or Tuesday work better?" (always A or B)
- Objection ladder: soft no → ask why. Price → $20 review discount first, $50 travel waive second (never both at once), + "satisfaction guarantee — we make sure you love everything before we leave." Non-price → new A/B days. Only stop after both levers deployed.

### Harris Bros AI config
- Ask "how many windows?" (not panes — customers don't know panes). Estimate 2 panes/window.
- Same checking route flow + <<HOLD>> 3-min delay
- Same conditional close: "If I can get you on the route I have in [city] this week..."
- Same A/B day forcing, same objection ladder, same guarantee
- Quarterly plan: pitch AFTER booking confirmed — do not lead with it

### <<HOLD>> mechanism (conversation.ts)
- Hayden can split a response with `<<HOLD>>` token
- Part before → sends immediately (the "checking route" holding message)
- Part after → setTimeout 3 min, sends as a separate assistant message, logged to DB + mirrored to Discord
- Committed in swell repo, deployed to Replit

---

## WPW / HH Backend system — big build day

### Architecture doc
Full plan written to: `/Users/zak/.openclaw/workspace/WPW_CONVERSATIONS_ARCHITECTURE.md`

### What was built (subagents, deployed to Replits)
- HH backend Replit SSH: `ssh -i ~/.ssh/replit_wolfpackwash -p 22 01616f2b-facf-41fc-a607-25a0ebe18b96@01616f2b-facf-41fc-a607-25a0ebe18b96-00-3gcvfx18qfig3.riker.replit.dev`
- D2D app Replit SSH: `ssh -i ~/.ssh/replit -p 22 147bd261-da64-4ba4-b9b6-f72f0845aff2@147bd261-da64-4ba4-b9b6-f72f0845aff2-00-x3gveabfcka4.janeway.replit.dev`

#### HH Backend new files/tables
- `hh_conversations` table — one row per customer conversation thread
- `hh_conversation_messages` table — every SMS logged
- `hh_rep_earnings` table — rep commission tracking
- `hh_tech_earnings` table — technician commission tracking
- `src/services/conversations.ts` — createOrFindConversation, logMessage, openDiscordChannel, postToDiscordChannel, scheduleOpeningMessage (60s delay), scheduleFollowUps (2h/24h/72h), handleInboundSms
- `src/services/rep-commissions.ts` — calculateAndLogEarning, commission configs
- `src/routes/conversations.ts` — GET /api/conversations, GET /api/conversations/:id, POST /api/conversations/:id/reply
- `src/routes/earnings.ts` — GET /api/earnings/me, PATCH /api/earnings/:id/mark-serviced
- Updated `src/routes/facebook.ts` — FB lead → createOrFindConversation → scheduleOpeningMessage(60s) → scheduleFollowUps
- Updated `src/routes/nurture.ts` — inbound SMS checks conversations first before nurture engine

#### Opening SMS template (WPW FB leads, Matthew's voice)
"Hey [FirstName], it's Matthew with Wolf Pack Wash 🐺 Thanks for reaching out about your house wash inquiry. Has it been more or less than a year since it was done?"
- 60-second delay after lead created
- Follow-ups if no response: 2h, 24h, 72h (templated, not AI)
- Discord channel created per customer under CONVERSATIONS category

#### Discord Conversations setup
- Structure: WPW server → CONVERSATIONS category → one text channel per customer
- Channel naming: `firstname-lastfour` (e.g. `mike-j-5614`)
- Customer info embed posted as first message in channel, @Matthew
- Two-way bridge: Discord message in channel → Twilio SMS to customer, customer SMS → posts to Discord channel
- **ENV VARS NEEDED** (add to HH backend Replit Secrets):
  - `DISCORD_WPW_GUILD_ID` = `1439371490947633202` ✅ (verified = Wolf Pack Wash server)
  - `DISCORD_WPW_CONVERSATIONS_CATEGORY_ID` = **TODO: Zak needs to create "CONVERSATIONS" category and copy ID**
  - `DISCORD_MATTHEW_USER_ID` = **TODO: Matthew's Discord user ID**

#### Commission structures
**Matthew** (matthewlinder123@gmail.com):
- D2D sales: 30% of job
- Ad leads he schedules: 10% of FINAL job value (including upsells)
- Reactivation — he personally closes: $20 flat + 10% of upsell above base
- Reactivation — customer books via link, he confirms: $10 flat + 10% upsell
- Leads + Conversations tab access (Matthew-exclusive)

**Judah** (email TBD — need to add as role="rep" in DB):
- D2D: 20% flat, 25% if hits 2 sales in a day
- No scheduling bonus
- Canvass + Routes + Earnings only (no leads/conversations)

**Nassim** (tech): 25% of job
**Harrison** (tech): 20% placeholder (configurable)
**Cameron** (tech): 20% placeholder (configurable)
- Payout: 3 business days after job marked serviced
- Mark serviced → auto-triggers review SMS next day

#### Expo App (D2D) — new screens deployed
- `MatthewTabNavigator.tsx` — Canvass, Leads (Field+Ad tabs), Conversations, Routes, Earnings, Profile
- `RepTabNavigator.tsx` — Canvass, Routes, Earnings, Profile (Judah + other D2D reps)
- `TechTabNavigator.tsx` — Jobs, Route (map), Earnings, Clock, Profile (Nassim/Harrison/Cameron)
- `screens/rep/RepLeadsScreen.tsx` — two tabs: Field Leads + Ad Leads, editable price, status changes fire to Meta CAPI
- `screens/rep/ConversationsScreen.tsx` — list of customer SMS threads, unread badges, 10s auto-refresh
- `screens/rep/ConversationDetailScreen.tsx` — chat UI, customer info header, reply box → SMS + Discord
- `screens/rep/EarningsScreen.tsx` — summary cards, earnings list, Mark Serviced button, commission info section
- `screens/tech/TechRouteScreen.tsx` — map + today's jobs list, weather strip, tap to navigate to job
- `screens/tech/TechEarningsScreen.tsx` — read-only earnings, summary, grouped by month
- Updated `TechJobStartScreen.tsx` — job notes, before checklist (gates Begin button)
- Updated `TechJobCompleteScreen.tsx` — after checklist (gates Complete button)
- RootStackNavigator: Matthew email → MatthewTabNavigator, role=rep → RepTabNavigator, role=tech → TechTabNavigator

### What still needs to happen
- [ ] Zak: create "CONVERSATIONS" category in WPW Discord → get ID → add as `DISCORD_WPW_CONVERSATIONS_CATEGORY_ID` in HH backend secrets
- [ ] Zak: get Matthew's Discord user ID → add as `DISCORD_MATTHEW_USER_ID`
- [ ] Judah's email needed to set role="rep" in users DB
- [ ] All 3 projects republished (done ~7:50pm EDT)

---

## Swell Schedule page
- Pending Bookings queue above calendar (ready-to-book leads)
- Route Builder tab with Leaflet map, job pins, travel time calc, book + SMS flow
- Scheduling SMS template: "Hey [FirstName], this is Hayden with [BizName]! Just a heads up — we'll be there [Day] [TimeWindow]. If you could please have any vehicles moved out of the driveway beforehand, that'd be great. See you then! 🙌"
