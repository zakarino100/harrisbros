
## 2026-04-30 — WPW Facebook lead sync (built + ready to publish)

- Subagent built initial `/sync-pulls-all` and `/sync-push-status` endpoints + dashboard buttons but had bugs:
  - missing `CRM_TO_META_EVENT` import in `facebook.ts` (build-breaking)
  - missing `Download`/`Upload` icon imports in `leads.tsx` (build-breaking)
  - `err.message` without TS narrowing
  - prompted user for `FORM_SUBMIT_SECRET` and `form_id` on every click (annoying)
  - silent backfill — Zak explicitly wants Discord notifications for any leads that missed the live webhook
- I patched all of the above:
  - unified `isAdmin()` helper accepts cookie OR token, dashboard now uses cookie auth (no prompts)
  - `form_id` defaults to `FACEBOOK_LEAD_FORM_ID` env var
  - `sync-pulls-all` fires `notifyNewLead` for every brand-new insert (default ON)
  - added `GET /api/facebook/diag` so we can verify deploy config without leaking secrets
- Verified: zero TS errors in our two changed files; full `pnpm build` succeeds end-to-end.
- Committed as `5c413df feat(facebook): manual sync buttons + Discord backfill + diag endpoint` and `f519fc0 chore: bump deploy marker`.
- **Not yet deployed** — Replit needs the user to hit Publish. `git push origin main` from SSH is blocked by GitHub credential prompt (no creds in shell).
