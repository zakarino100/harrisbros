- Config update: OpenClaw default model routing changed to Haiku-first to save tokens. Primary set to `anthropic/claude-3-5-haiku-latest`, with fallbacks `anthropic/claude-sonnet-4-6` then `openai-codex/gpt-5.4`. Intent: use Haiku for everyday tasks; switch to Sonnet manually when heavy lifting is needed (complex coding, deep analysis, large tasks); GPT-5.4 as last resort.
- Fixed a config issue while doing that: previous default model entry referenced invalid/missing `anthropic/claude-sonnet-4-64`; config now validates successfully.
- Reminder scheduled for Zak tonight at 9:00 PM local time in the main session. Cron job id: `94230f01-594b-4dc2-9af1-964549ae97cd`.
- Memory search tool was unavailable during this session because embedding quota was exhausted (OpenAI insufficient_quota / 429), so continuity was written manually to daily memory.
- Switched memory embedding provider from OpenAI (quota exhausted) to Voyage AI. Key stored in ~/.openclaw/agents/main/agent/auth-profiles.json and VOYAGE_API_KEY added to launchd plist. Config: agents.defaults.memorySearch.provider = "voyage". Using voyage-4-large model. 200M free token allowance — effectively unlimited at current scale. Embeddings confirmed ready after gateway restart.
