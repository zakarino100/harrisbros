# 2026-04-28

## Resend webhook + Supabase password rotation

Zak rotated the Supabase DB password. New connection details:
- Host: `db.hclpovktywijfnswthpm.supabase.co`
- Password: `Eaglesfan1998$`
- Connection string: `postgresql://postgres:Eaglesfan1998$@db.hclpovktywijfnswthpm.supabase.co:5432/postgres`
- ⚠️ Note: this project ref (`hclpovktywijfnswthpm`) is different from the Finish Line ref (`isybxezvcqduizarhruf`) we had in memory. Need to confirm with Zak whether this is a fresh HH backend project or the existing one with a new view.

Zak gave me the Resend API key: `re_N6A8svsN_3YX9jLE4XwMC7a7BsrkZgzCf`. I:

1. **Audited the HH backend** at `~/.openclaw/workspace/hh-backend-fresh` (mirror of Replit `healthy-home-backend.replit.app`).
2. **Found the events webhook handler already exists** at `POST /api/activity/webhooks/resend` — wired to `handleResendWebhook()` in `services/resend.ts`. Handles `email.opened`, `email.clicked`, `email.bounced`, `email.complained` and updates `campaign_sends` + `lead_activity` tables.
3. **Found the gap**: route was NOT in the auth-public-paths list, so it was returning 401 to Resend. Fixed in `artifacts/api-server/src/app.ts` — added `/api/activity/webhooks/resend` to `PUBLIC_PATHS`.
4. **Registered the webhook with Resend** via API:
   - Webhook ID: `2db93fdb-de60-4ddc-b317-bedf877440bf`
   - Endpoint: `https://healthy-home-backend.replit.app/api/activity/webhooks/resend`
   - Signing secret: `whsec_tQDarHlih21eI3L02xIuSmmNBAkijiK9`
   - Events: sent, delivered, delivery_delayed, bounced, complained, opened, clicked, failed
5. **HMAC verification NOT implemented** in code yet — `RESEND_WEBHOOK_SECRET` is documented in the resend service header but never read/used. Hardening pass deferred. For now the webhook accepts any payload posted to the endpoint, which is fine for an MVP but should be tightened.
6. **🚨 Resend has zero verified domains.** `wolfpackwashnc.com` is not added to the Resend account. Until that's verified with DNS records, no campaign emails will send. This is the next blocker.

## Replit secrets Zak needs to update/add

- `DATABASE_URL=postgresql://postgres:Eaglesfan1998$@db.hclpovktywijfnswthpm.supabase.co:5432/postgres`
- `RESEND_WEBHOOK_SECRET=whsec_tQDarHlih21eI3L02xIuSmmNBAkijiK9` (currently unused but stash for when we add HMAC)
- Confirm `RESEND_API_KEY=re_N6A8svsN_3YX9jLE4XwMC7a7BsrkZgzCf` is set
- Confirm `EMAIL_FROM_ADDRESS=contact@wolfpackwashnc.com` and `EMAIL_FROM_NAME=Luna @ Wolf Pack Wash` are set (the code defaults to `hello@wolfpackwashnc.com` / `Wolf Pack Wash`, which is wrong for the Luna identity).

## What's next

1. Zak deploys the Replit code change (auth public-paths edit) so the webhook stops 401-ing.
2. Add and verify `wolfpackwashnc.com` in Resend (DNS records: SPF/DKIM/DMARC).
3. Smoke-test by sending one email through the campaign sender and verifying the webhook fires + the `campaign_sends` row updates.
4. Then we can proceed to Phase 2 work (Google Calendar slot picker, etc.) and the Twilio backup webhook discussion.

---

## Update 08:10 — follow-ups

**Finish Line cleared from memory.** Zak confirmed he's not working with that company anymore. Removed Finish Line bot/Supabase/pricing details from MEMORY.md, kept only the partnership-with-Nevaeh line since that still matters for Blue Ocean.

**HMAC implemented.** Built `lib/svix-verify.ts` (native `crypto`, no new deps) — Svix-compatible signature verification (id + timestamp + signature, HMAC-SHA256, base64, replay window 5 min, supports rotated keys). Wired into `routes/activity.ts` webhook handler. Behavior: if `RESEND_WEBHOOK_SECRET` is set, verify and 401 on mismatch; if unset, log a loud warning and accept (so first deploy doesn't drop events while secret is being added). Typecheck has many pre-existing errors but zero new errors from my code.

**Resend domain situation.** The apex `wolfpackwashnc.com` is already registered to a different Resend account (probably an older account Zak forgot about, or a previous freelancer). Tried adding it on his account: 403 "already registered". Workaround: created `send.wolfpackwashnc.com` as a sending subdomain (id `44127ba1-43a8-421b-9baf-a4c400a9bfab`). This is actually Resend's recommended pattern — keeps mail reputation isolated from the apex.

**DNS records Zak needs to add at Squarespace** (for `send.wolfpackwashnc.com`):

| Type | Name | Value | Priority |
|---|---|---|---|
| TXT (DKIM) | `resend._domainkey.send` | `p=MIGfMA0GCSqGSIb3DQEBAQUAA4GNADCBiQKBgQChaCh2LVG3eHAs9f/jmcf+33umIp9PSAP2RzClXMOcFwHBoyeJS34O9SPCuS7tTNC2djOxK+fKSW9WeAVZr747wlxK6URZKiCgWiaufuj2Dac72b8PWogEOD8s+p1TlTIPLQwpDXBuAd3GKY+fF4NtsaafAQPJagDEtqPaLlbWFwIDAQAB` | — |
| MX (SPF) | `send.send` | `feedback-smtp.us-east-1.amazonses.com` | 10 |
| TXT (SPF) | `send.send` | `v=spf1 include:amazonses.com ~all` | — |

The `send.send` is intentional — Resend appends the host name itself, and Squarespace usually wants the host part only (not full FQDN).

**On the apex situation:** worth chasing later. If Zak can find that other Resend account (try password reset on common emails), he can delete the domain there and re-add on the active account. Not a blocker for launch.

**Squarespace access:** I can't log in to Squarespace remotely (2FA + session tokens), so I gave Zak the records to paste in. If he wants me to verify the domain after he adds them, I just have to call the Resend `verify` endpoint.
