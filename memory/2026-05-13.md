# Memory — May 13, 2026

## MackWash Receptionist Mode Overhaul

### What changed
- Zak asked to switch MackWash Hayden from quote-and-close → receptionist-only mode
- Hayden now: greets, gathers service type + address + last done date + property details + special requests, then hands off to Mack with `<<HANDOFF: info_complete>>`
- Hayden does NOT quote, price, discount, or book — all of that is Mack's job
- Model: `claude-haiku-4-5` (simple intake, no need for Sonnet)
- Sonnet is for MY work (Robin, this session), not for MackWash Hayden

### Code changes made (local workspace, NOT yet deployed to Replit)
- `swell/server/services/conversation.ts`: Added `receptionist` mode branch in `buildSystemPrompt()` — returns early with clean intake-only system prompt, never reaches quoting/pricing/save-the-sale sections
- `swell/server/db/queries.ts`: Removed auto-mirror Discord post from `insertConversationMessage()` — this was causing DOUBLE messages in Discord (💬 Customer + 💬 Lead both posting for every inbound SMS)
- DB (Supabase): `swell_ai_configs` for mackwash — set `mode='receptionist'`, `model_primary='claude-haiku-4-5'`, cleared `custom_brand_notes`
- Both files compiled clean

### Bugs identified & fixed (pending Replit deploy)
1. **Double Discord messages** — two code paths both mirroring SMS to thread. Fixed by removing auto-mirror from `insertConversationMessage()`. Only explicit `mirrorSmsToThread()` calls in conversation.ts remain.
2. **Hayden meta-response** — "I need to clarify: I should only respond when the customer sends a message..." — AI broke character when test messages piled up with no responses (due to Twilio 401). Need to add guardrail to receptionist prompt.
3. **Test leads 60 & 61** (phones +15550000099, +15559990001) — fake leads from testing, need to be cleaned up from DB and Discord once Supabase is back up.

### Root cause of test failures
- **Supabase was paused** (unpaid bill) — caused DB connection failures during the test run
- **Twilio 401** — test was using fake/test credentials so SMS sends failed
- Zak paid the Supabase bill (~3:00 PM) and added Twilio credits
- Once Supabase comes back online, need to: clean test leads, deploy code to Replit, add meta-response guardrail

### Pending deploy to Replit
- Production Swell server runs on Replit at nopressurelaunch.com
- Local code changes are ready but NOT pushed to production yet
- Need to sync updated `conversation.ts` and `queries.ts` to Replit and restart server

## Owner Chat for Mack
- Mack can text his MackWash number with questions about leads/pipeline stats
- Already live — `handleOwnerSms()` in conversation.ts handles this
- Logged to Discord #leads channel
- Discord owner-chat channel exists in MackWash server: #owner-chat

## Swell General State (May 13)
- MACKWASH: active, Supabase recovering, Twilio credits restored
- HARRIS_BROS: disabled (AI off, password changed to nopressurelaunchstop)
- Discord bot token in .env: `[token-redacted]`
- MackWash leads channel: 1499992549727932436
- MackWash guild: 1499992504370729022

## Recent MackWash leads (May 11-13, need conversion status from Mack)
- Brian, Russ Sharpe, Robin Riggsbee, Chante Freeman, Lokesh Mondal, Katrelle Pollard, Rick Cairnes
- Mack said he closed 2 leads from yesterday — need to identify which and mark in CRM for Facebook CAPI conversion data
- These leads could not be texted due to Twilio credit outage — do NOT send retroactive messages to them

## Files created this session
- `MACKWASH_CONFIG_SUMMARY.md` — full documentation of receptionist mode change
- `NEXT_STEPS.md` — actionable items for Zak
