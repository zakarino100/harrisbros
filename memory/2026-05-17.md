# Memory — 2026-05-17

## Discord ↔ Web Chat Integration — CONFIRMED WORKING
- Both Discord and web chat route to the same Robin main session
- Memory is fully shared across channels
- Zak confirmed Discord → Robin bot is live (tested 5:44 PM EDT May 17)
- Zak can message Robin in Discord for work requests from anywhere

## Showroom Auto Styles — Full Investigation Complete (May 17)
- SSH: `ssh -i ~/.ssh/replit -p 22 411651c9-c83c-485a-a66d-32c090c1cabb@411651c9-c83c-485a-a66d-32c090c1cabb-00-32ng8yz4p8a0u.kirk.replit.dev`
- Tech: React+Vite frontend, Express+Supabase backend, port 5000
- Build: `npm run build` → `node ./dist/index.cjs`
- All secrets in Replit Secrets (not .env file)
- Has: Leads CRM, IVR (Eduardo→Juan ring order), voicemail, call transcription, S3 recordings, background poller (15s), SMS conversations
- Intake agent: rule-based (disabled by default) — gets vehicle year/make/model/services from form already
- Follow-up scheduler: Day 2/5/10 sequence (disabled by default)
- Google Ads: NOT yet wired — needs to be added to COMMAND
- Hayden plan: Simple confirmation SMS only (vehicle info already comes from form)
  - Message: 'Hey [name]! Thanks for reaching out to Showroom Auto Styles! We received your [service] inquiry for your [year] [make] [model]. Our team will reach out shortly with your personalized quote.'
- 5 open questions sent to Zak via Discord DM (Hayden bot): Discord server, Eduardo/Juan IDs, Twilio number, Google Ads account ID, Replit secrets dump
- Waiting on Zak's Discord reply before starting Railway migration

## Architecture Plan (finalized May 17)
- Blue Ocean Digital Marketing = umbrella company
- COMMAND = Zak's internal super-admin tool (command.blueocean-dm.com)
  - Social posting (FB, IG, GBP), ad metrics, Google Reviews widget, AI captions
  - DB: separate Postgres + Supabase
  - Env needs: META tokens per brand, GBP OAuth, Outscraper, OpenAI, Discord webhook
  - Ready to migrate to Railway NOW (no blockers)
- No Pressure Launch = client-facing SaaS platform
  - Swell = the tech powering it (mackwash.nopressurelaunch.com)
- Blue Ocean site (blueocean-dm.com) = client portal + marketing site
  - BLOCKER: Uses Replit Auth (OIDC) — must replace with Supabase Auth before Railway migration
- Swell GitHub: zakarino100/harrisbros → auto-deploys to Railway on push

## Mop Mafia Plan
- Enable as full Swell tenant (already seeded as mop_mafia, currently disabled)
- Keep Gia as the AI persona (NOT Hayden)
- Wire into Swell's A/B engine, pre-send guard, Discord handoff
- Migrate Mop Mafia CRM (worf Replit) to Railway after Swell tenant is enabled
- Mop Mafia landing page (spock): static Vite site at book.mop-mafia.com

## COMMAND Investigation Results
- ROOT codebase (/workspace/artifacts/) is the canonical one — has Ads, Reviews, Embed (COMMAND folder is stripped version)
- Reviews = Google Reviews via Outscraper API scraper, publish-gated, embeddable widget
- Ads = Meta + Google ad account mgmt, daily metrics (spend, ROAS, CPL, CTR)
- REPLIT_DOMAINS env var used for CORS — needs update on Railway
- Per-brand Meta tokens hardcoded as env vars (needs updating)

## A/B Optimization Engine (deployed May 17)
- swell_variant_messages table: 22 messages seeded (opener + 10 stages x A/B)
- swell_ab_test_history table: logs each cycle result
- Minimum 15 sends per variant before declaring winner
- Within 2% reply rate = tie, keep testing
- Winner declared → Haiku generates new challenger → updates DB → logs history → DMs Zak
- Every 2 days at 8 PM EDT cron fires the analysis + optimization loop
- All variant messages DB-driven (no code changes needed to update copy)

## Opening Message A/B Templates (Showroom/Swell)
- Variant A: Uses first name, acknowledges price inquiry, asks last wash year
- Variant B: Casual, direct to qualification question
- Both: first name only (split full_name), no LLM cost, pre-send guard runs
- model_used field set to opener-template-A or opener-template-B for tracking

## Subagent Model Selection Rule (set by Zak 2026-05-17)
- **Haiku** → basic/simple tasks (data lookups, simple scripts, quick checks)
- **Sonnet** → advanced tasks (multi-step logic, code changes, analysis)
- **Opus** → super advanced tasks (complex architecture, deep reasoning, high-stakes planning)
- Goal: save credits over time by not over-using expensive models
- Apply this every time a subagent is spawned


## Swell — Railway Migration (COMPLETE)

- Swell is now live on Railway: **https://swell-production.up.railway.app**
- GitHub repo: **https://github.com/zakarino100/harrisbros** (was stale, now updated)
- Railway service: `swell` | service ID: `5bbf3311-4bb6-424c-b1a8-4a20c17e9478`
- Railway project: `production` environment
- Node 20 required (Tailwind v4 / @tailwindcss/oxide); added `.nvmrc` and `nixpacks.toml` to force it
- Auto-deploys on every `git push` to harrisbros repo
- Replit is now abandoned for Swell — no more deployments there
- `swell.nopressurelaunch.com` DNS still needs to be pointed to Railway (pending)

## Twilio Webhooks Updated → Railway

- MackWash `+17704158392` → `https://swell-production.up.railway.app/api/twilio/inbound`
- Harris Bros `+14174572644` → `https://swell-production.up.railway.app/api/twilio/inbound`

## FB Webhook — Manual Step Pending

- Needs to be updated in Meta Developer Portal manually (app secret not in env, can't do programmatically)
- New URL: `https://swell-production.up.railway.app/api/facebook/webhook`
- Verify token: `e0678716945b9922e76fea22ca0bb08642478bcf960c0df046d63f43a41097a4`

## Hayden Discord Bot Token Fix

- Bot was failing with 4004 (auth failed) — token was dead
- Zak regenerated token in Discord Developer Portal
- New token deployed to Swell (Railway env + previously to Replit secret)
- Token confirmed valid: bot ID `1499988099428782172`
- Discord gateway now connected

## Swell Code Fixes Deployed Today

### BUG FIXES (all deployed to Railway):
1. **Nurture loop receptionist mode guard** — `fireNurtureJob` now returns early if tenant mode is `receptionist`. Prevents AI refusal messages from being sent as SMS to customers.
2. **Pre-send guard** — New `server/services/pre-send-guard.ts` module. Runs before EVERY outbound SMS. Checks: empty message, AI meta-messages (15 patterns), DNC, prompt leakage, receptionist no-pricing, receptionist no-scheduling-confirmations. Messages blocked are logged with `error='_blocked: reason'` and never touch Twilio.
3. **Discord thread internal/SMS distinction** — `mirrorSmsToThread` now takes `sentViaSms` param. Internal AI messages show as `🔒 [Internal — not sent to customer]`. Actual SMS shows as `🤖 Hayden — ✅ SENT`.
4. **AI cost removed from dashboard** — `cost_cents`, `tokens_in`, `tokens_out` stripped from all API responses. Mack (tenant) cannot see AI costs.
5. **Hidden refusal messages** — 31 garbled "I appreciate the instruction" messages marked `error='_hidden'` in DB. API route filters them out. Mack sees nothing in CRM.
6. **Error filter in API** — `/api/messages/:id` and conversation list query both filter `error='_hidden'` messages from responses.

### AFFECTED LEADS — SMS failure cleanup:
- 11 MackWash leads received garbled AI refusal SMS texts
- All 11: AI paused (`ai_paused=true`), status set to `stopped`, activity note logged: "AI auto-responder deactivated. Lead received incorrect SMS messages due to system error. Manual follow-up required."
- No apology SMS sent — Zak's decision
- Nurture jobs for all 11 cancelled

## Discord Backfill — MackWash

- 6 MackWash leads were missing Discord threads — backfilled via `discord-backfill-missing.mjs`
- Script is idempotent — safe to run again; skips leads with existing `discord_thread_id`

## Service Map Rewrite

- Filters changed: removed "Hot" (🔥), replaced with: All / 🆕 New Leads / 💬 Contacted / ✅ Serviced
- Pin colors: Blue = New, Amber = Contacted, Green = Serviced
- Auto-zoom to bounds of all geocoded pins on load
- Race condition fixed: `mapReady` state flag ensures pins render even if loaded before map initialized
- "View Lead →" button in popup — navigates to Leads tab and opens lead drawer
- Default center: Atlanta/Douglasville area (Mack's market) while pins load
- Legend updated to match new categories

## Railway Config Files Added to harrisbros repo

- `railway.json` — build/deploy config
- `.nvmrc` — Node 20
- `nixpacks.toml` — force Node 20 via Nix
- `.gitignore` — excludes node_modules, dist, .env, *.db

## Token Health Check Cron

- Hourly cron configured to check Discord bot token validity
- Alerts Zak in Discord if token is dead
- Prevents multi-day outages like the one today

## Swell DB Notes

- `swell_conversation_messages` has `error` column — used for `_hidden` (filtered from UI) and `_blocked: reason` (guard blocks)
- `swell_nurture_jobs` — cancelled 11 scheduled jobs for affected leads; also cancelled all mackwash pending/scheduled on reset
- `discord_thread_id` stored on both `swell_leads` and `swell_conversations`

## MackWash Tenant Status

- Enabled in DB ✅
- FB form ID: `1177015427806726`
- GUILD_ID: `1499992504370729022`
- LEADS_CHANNEL_ID: `1499992549727932436`
- Twilio from: `+17704158392`
- AI mode: receptionist (gather info, no quotes, handoff only)

## Harris Bros Tenant Status

- Disabled in DB (Harris Bros is not an active engagement right now)
- Password changed to `nopressurelaunch_stop`
